<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MuseProgressBar" parent="@style/Widget.AppCompat.ProgressBar.Horizontal">
        <item name="colorButtonNormal">#f50057</item>
        <item name="colorControlActivated">#33691e</item>
        <item name="colorControlHighlight">#ff6d00</item>
        <item name="android:progressDrawable">@drawable/progress_bar</item>
        <item name="android:minWidth">200dp</item>
    </style>

    <style name="buttonText" parent="@style/TextAppearance.AppCompat.Button">
        <item name="android:capitalize">none</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="progressText" parent="@style/TextAppearance.AppCompat.Headline">
        <item name="android:textSize">37sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
